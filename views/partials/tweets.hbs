<div class="each-tweet">
    <img src="{{this.user.profile_image_url}}">
    <h2>{{this.user.name}}</h2>
    <p class="textt">{{this.text}}</p>
    <p>{{this.entities.description}}</p>
    <input type="checkbox" name="tweet-check" class="tweet-check">
    <label for="gui">Select tweets to advance</label>
</div>